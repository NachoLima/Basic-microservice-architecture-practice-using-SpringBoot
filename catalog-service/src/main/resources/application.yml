spring:
  application:
    name: catalog-service
  config:
    import: optional:configserver:http://localhost:8888
  mongodb:
    embedded:
      version: 4.0.2
  data:
    mongodb:
      database: Catalog
      port: 27017